pipeline {
    agent any

    tools {
        nodejs 'Node'
    }


    stages {
        stage ('login') {
        withCredentials([string(credentialsId: 'dockerlogin', variable: 'docker')]) {
            sh 'docker login -u kartikeya532001 -p ${docker}'
}
        }

      
    }
}
